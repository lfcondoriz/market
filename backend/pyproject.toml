[project]
name = "backend"
version = "0.1.0"
description = "Market data backend"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
market = "app.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]